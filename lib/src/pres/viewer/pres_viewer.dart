import 'package:flutter/material.dart';

class PresViewer extends StatefulWidget {
  const PresViewer({super.key});

  @override
  State<PresViewer> createState() => _PresViewerState();
}

class _PresViewerState extends State<PresViewer> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
